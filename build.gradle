buildscript {
    ext.kotlin_version = '1.2.51'

    repositories {
        mavenCentral()
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

apply plugin: 'idea'

defaultTasks 'install', 'installDist'

subprojects {

    repositories {
        mavenCentral()
        mavenLocal()
    }

}

project(':tedi-core') {

    apply plugin: 'kotlin'
    apply plugin: 'maven'

    version = '0.1'
    group = 'uk.co.nickthecoder'

    compileJava {
        options.compilerArgs << '-Xlint:unchecked'
    }

    dependencies {
        compile "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    }

    defaultTasks 'install'
}

project(':demo') {

    apply plugin: 'kotlin'
    apply plugin: 'application'
    
    mainClassName = "uk.co.nickthecoder.tedi.demo.DemoKt"

    dependencies {
        compile project(':tedi-core')

        testCompile 'junit:junit:4.9'
        testCompile 'org.jetbrains.kotlin:kotlin-test'
    }
}

project(':example') {

    apply plugin: 'kotlin'
    apply plugin: 'application'
    
    mainClassName = "uk.co.nickthecoder.tedi.example.Example"

    dependencies {
        compile project(':tedi-core')

        testCompile 'org.jetbrains.kotlin:kotlin-test'
    }
}
